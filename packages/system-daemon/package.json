{
  "name": "@blockingmachine/system-daemon",
  "version": "1.0.0-rc.3",
  "description": "High-throughput loopback DNS filtering proxy and system-level endpoint protection daemon for Blockingmachine.",
  "type": "module",
  "bin": {
    "blockingmachine-daemon": "./bin/blockingmachine-daemon.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "start": "node dist/index.js",
    "test": "node --experimental-vm-modules ../../node_modules/jest/bin/jest.js",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@blockingmachine/core": "^1.0.0-rc.3",
    "dns-packet": "^5.6.1",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^26.6.2",
    "rimraf": "^6.1.3",
    "typescript": "~5.8.3"
  }
}
